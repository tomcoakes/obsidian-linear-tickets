{
  "name": "obsidian-linear-tickets",
  "version": "0.2.1",
  "description": "Turns Linear ticket IDs in Obsidian notes into links with hover cards.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "license": "MIT",
  "devDependencies": {
    "@codemirror/language": "^6.12.4",
    "@types/node": "^22.20.3",
    "esbuild": "^0.28.2",
    "obsidian": "^1.13.1",
    "typescript": "^7.0.2"
  }
}
